<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Nembak Rina</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      color: #fff;
      text-align: center;
      overflow: hidden;
    }

    .container {
      background: rgba(255, 255, 255, 0.1);
      padding: 50px 30px;
      border-radius: 25px;
      box-shadow: 0 8px 30px rgba(0,0,0,0.2);
      backdrop-filter: blur(10px);
    }

    h1 {
      font-size: 3rem;
      margin-bottom: 20px;
      animation: fadeIn 2s ease-in-out;
    }

    p {
      font-size: 1.5rem;
      margin-top: 0;
      animation: fadeIn 3s ease-in-out;
    }

    .heart {
      font-size: 3rem;
      color: #ff3860;
      animation: pulse 1s infinite;
      display: inline-block;
      margin: 20px 0;
    }

    button {
      background: #ff3860;
      border: none;
      padding: 15px 30px;
      border-radius: 10px;
      color: #fff;
      font-size: 1.2rem;
      cursor: pointer;
      margin: 10px;
      transition: transform 0.2s;
    }

    button:hover {
      transform: scale(1.1);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.3); }
      100% { transform: scale(1); }
    }

    .confetti {
      position: fixed;
      width: 10px;
      height: 10px;
      background: #fff;
      pointer-events: none;
      animation: fall 3s linear forwards;
    }

    @keyframes fall {
      0% { transform: translateY(0) rotate(0deg); opacity: 1; }
      100% { transform: translateY(100vh) rotate(360deg); opacity: 0; }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Rina 💖</h1>
    <p>yeeay 😍 kita resmi ya!</p>
    <div class="heart">❤️</div>
    <div>
      <button id="iyaBtn">Iya 😍</button>
      <button id="nggakBtn">Mau nggak? 😅</button>
    </div>
  </div>

  <script>
    const iyaBtn = document.getElementById('iyaBtn');
    const nggakBtn = document.getElementById('nggakBtn');

    iyaBtn.addEventListener('click', () => {
      for(let i=0; i<50; i++){
        const confetti = document.createElement('div');
        confetti.classList.add('confetti');
        confetti.style.left = Math.random() * window.innerWidth + 'px';
        confetti.style.background = `hsl(${Math.random() * 360}, 70%, 60%)`;
        confetti.style.width = confetti.style.height = Math.random() * 10 + 5 + 'px';
        document.body.appendChild(confetti);
        setTimeout(() => confetti.remove(), 3000);
      }
      alert('Yeay! Kita resmi ya 😍💕');
    });

    nggakBtn.addEventListener('click', () => {
      alert('Waduh 😅 jangan sedih ya, bossku!');
    });
  </script>
</body>
</html>
